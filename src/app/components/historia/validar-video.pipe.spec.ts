import { ValidarVideoPipe } from './validar-video.pipe';

describe('ValidarVideoPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidarVideoPipe();
    expect(pipe).toBeTruthy();
  });
});
